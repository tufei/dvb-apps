Requirements:

- ttusb_dec_reset depends on libusb; to build it
  you have to specify ttusb_dec_reset=1 (see below)

Building:

simply type
$ make

to also build ttusb_dec_reset type
$ make ttusb_dec_reset=1

Installing:

$ make install
installs libraries and utils to /usr/[bin,include,lib]

$ make install prefix=<path>
installs libraries and utils to <path>/[bin,include,lib]
